[alembic]
script_location = %(here)s/alembic
# Files are named <revision>_<slug>.py, for example 0002_add_books.py.
# Pass --rev-id with the next number when creating a revision.
file_template = %%(rev)s_%%(slug)s
prepend_sys_path = .
path_separator = os
# The database URL comes from app settings (DATABASE_URL); see alembic/env.py.
sqlalchemy.url =

[post_write_hooks]
# New revision files are lint-fixed and formatted like the rest of the code.
hooks = ruff_fix, ruff_format
ruff_fix.type = module
ruff_fix.module = ruff
ruff_fix.options = check --fix --exit-zero REVISION_SCRIPT_FILENAME
ruff_format.type = module
ruff_format.module = ruff
ruff_format.options = format REVISION_SCRIPT_FILENAME

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
