"""The Copilot: one assistant with a face per role (a system prompt, a set of tools and the
signed-in user's own permissions), a tool-calling loop over a language model, and the checks on
what it may say. The app works fully without it.
"""
